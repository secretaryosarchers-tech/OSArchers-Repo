{
  "name": "oswebsite",
  "version": "1.0.0",
  "description": "OSA Club Website",
  "scripts": {
    "start": "live-server --port=3000",
    "dev": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
